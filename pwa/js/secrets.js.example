/**
 * secrets.js — Tokens et clés API privées (NE PAS COMMITER)
 *
 * Instructions :
 *  1. Copier ce fichier : cp secrets.js.example secrets.js
 *  2. Remplir votre token Météo-France ci-dessous
 *  3. Le fichier secrets.js est gitignore — il ne sera jamais publié sur GitHub
 *
 * Token gratuit : https://portail-api.meteofrance.fr/
 *   → Créer un compte → Mes applications
 *   → S'abonner à "AROME" ET "AROME Prévision Immédiate"
 *     (un seul token couvre les deux APIs)
 */

// Token API Météo-France — couvre AROME (+42h) et AROME-PI (nowcasting 0–6h)
CONFIG.METEO_FRANCE.token = 'VOTRE_TOKEN_METEOFRANCE_ICI';
